import React from 'react';

// This component has been removed to comply with the API key handling guidelines.
// The API key must be provided exclusively through the `process.env.API_KEY` environment variable.
// The application must not ask the user for the API key.
const ApiKeyModal: React.FC = () => {
  return null;
};

export default ApiKeyModal;
